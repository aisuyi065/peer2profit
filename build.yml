version: '3.8'
services:
  Peer2Profit:
    build:
      context: .
      dockerfile: Dockerfile
    image: enwaiax/peer2profit
    restart: unless-stopped
    command: ["/app/p2pclient", "-l", "${email}"]
  Peer2Profit2:
    build:
      context: .
      dockerfile: Dockerfile
    image: enwaiax/peer2profit
    restart: unless-stopped
    command: ["/app/p2pclient", "-l", "${email}"]
  Peer2Profit3:
    build:
      context: .
      dockerfile: Dockerfile
    image: enwaiax/peer2profit
    restart: unless-stopped
    command: ["/app/p2pclient", "-l", "${email}"]
